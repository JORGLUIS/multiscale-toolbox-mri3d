[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "multiscale-toolbox"
version = "0.4.0"
description = "Reusable multiscale image processing toolbox aligned with Tarea2_.ipynb for WavePsi/HaarPSI analysis and DWT2 denoising in 2D and 3D."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "numpy",
  "scipy",
  "pandas",
  "matplotlib",
  "pillow",
  "nibabel",
  "PyWavelets",
  "scikit-image",
]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
